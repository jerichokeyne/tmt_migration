[project]
name = "tmt-migration"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "lxml>=6.1.2",
    "pylero>=0.2.0",
]
