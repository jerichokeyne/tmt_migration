# Test

## Step

Create HCP cluster with " --external-auth-providers-enabled" and cluster version lower than 4.15

## Expect

It will return clear and readable error message

## Step

Create non-HCP cluster with " --external-auth-providers-enabled"

## Expect

It will return clear and readable error message
